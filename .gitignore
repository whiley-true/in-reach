# Python
.venv/
__pycache__/
*.pyc
*.pyd
*.egg-info/
# The compiled _reachvarianttool module and the Qt runtime beside it are build output (native/build.py, and CI's
# native.yml for the published wheels), not source -- see native/README.md.
/src/in_reach/app/rvt/native/
.pytest_cache/
src/inreach.egg-info
    
# Environment variables
.env

# Local development
requirements.txt
.scripts/
refactor/
/.in-reach/

# Local/ephemeral Claude Code state
.claude/scheduled_tasks.lock
CLAUDE.local.md
CLAUDE.md
.claude/
PROMPT.md

# Vscode
.vscode/

# Dev
notes.txt

# Windows crash dumps (WER LocalDumps) -- large binaries, never meant to be committed
*.dmp
# Build output (python -m build / pip wheel, and CI's wheel step)
/build/
/dist/
/native/build/
